// This file is a placeholder for reportWebVitals
// In a real app, you would implement actual performance reporting here
const reportWebVitals = () => {
  // This function can be used to report web vitals to an analytics endpoint
  // For now, we'll just log to console in development
  if (process.env.NODE_ENV === 'development') {
    // Optional: log web vitals to console during development
    // console.log('Web Vitals: ', metric);
  }
};

export default reportWebVitals;